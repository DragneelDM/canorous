import { pageMetadata } from "src/data/metadata";
import UnrealPageContent from "./UnrealPageContent";

const metadata = pageMetadata.unreal;

export default function UnrealPage() {
  return <UnrealPageContent />;
}
